-- 사용자가 "유지 안 함"을 눌러 명시적으로 "더 이상 안 쓴다"고 표시한 시각. NULL이면 미확인 상태일
-- 뿐 실제로 안 쓴다는 뜻은 아니다 — 침묵을 "사용 안 함"으로 해석하면 안 되므로 이 컬럼으로 명확히
-- 구분한다. "이번 회차 확인"(유지하기)을 다시 누르면 NULL로 되돌아간다(재사용 재개로 간주).
ALTER TABLE purchases ADD COLUMN discontinued_at TEXT;
