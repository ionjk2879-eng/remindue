-- "삭제"(취소와 구분)를 위한 소프트 삭제 컬럼 — 목록/알림에서는 완전히 빠지지만, 이미 발생한
-- 지출로서 월별/연간 지출 통계에는 계속 집계된다(정기 항목은 이 시각 이후 회차만 제외).
-- 진짜 "취소"(주문 자체가 무효였거나 환불받아 지출로 칠 필요가 없는 경우)는 여전히 기존
-- DELETE FROM purchases(하드 삭제)를 쓴다 — 이번 컬럼과는 별개.
ALTER TABLE purchases ADD COLUMN discarded_at TEXT;
